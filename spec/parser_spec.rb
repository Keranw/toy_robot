require 'spec_helper'

describe Parser do

  describe "#new" do
    it "generate a parser object" do

    end
  end

  describe "#cmd" do
    it "skip a command when the command is invalid" do
      #@robot.cmd(["plase", [1, 1, "north"]])
      #expect(@robot.face_to).to eq("")
    end
  end

  describe "cmd" do
    it "skip commands before PLACE command" do
      #@robot.cmd(["move"])
      #expect(@robot.position).to eq([0, 0])
    end
  end

  describe "#parse" do
    it "return parsing result" do

    end
  end

  describe "#parse" do
    it "distingush commands and arguments" do

    end
  end

  describe "#parse" do
    it "won't accept invalid command" do

    end
  end

  describe "#parse" do
    it "won't accept invalid arguments" do

    end
  end

end
